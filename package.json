{
  "name": "osmf-evidence-graph",
  "version": "0.1.0",
  "private": true,
  "description": "OSMF Shared Evidence Graph schemas and git-authored seed dump",
  "license": "CC-BY-4.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:dump": "node scripts/build-dump.js",
    "validate:dump": "node scripts/validate-dump.js",
    "import:tracker": "node scripts/import_from_research_tracker.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
